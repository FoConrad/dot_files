" No compatibility – necessary for mappings to work.
set nocompatible

" Status line
set laststatus=0
set cmdheight=1
set nomodifiable " Only in version 6.0
set readonly

" Turn syntax on
syntax enable

" Key bindings.
nmap q :q!<CR>
nmap d <C-D>
nmap u <C-U>
nmap y <C-Y>
nmap e <C-E>

nmap s :set filetype=
cnoremap s/ s/\v
nnoremap / /\v
